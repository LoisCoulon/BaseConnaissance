<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="{{ path('knowledgesheet') }}"><img src="{{ asset('assets/image/logo.svg') }}"
                                                                         alt="Logo"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="#pageSubmenu" data-toggle="collapse" aria-expanded="false" class="nav-link mr-5">Admin</a>
                    <ul class="collapse list-unstyled" id="pageSubmenu">
                        <li>
                            <a href="{{ path('users_index') }}" class="nav-link">Utilisateurs</a>
                        </li>
                        <li>
                            <a href="{{ path('context_create') }}" class="nav-link">Contextes</a>
                        </li>
                    </ul>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if  is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') %}
                    <a class="nav-link" href="{{ path('users_edit', {'id': app.user.id}) }}">Mon Espace </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>



